<Window x:Class="Slik.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Slik"
        mc:Ignorable="d"
        Title="MainWindow"
        AllowsTransparency="True"
        WindowStyle="None"
        Height="900"
        Width="1600">
    <Grid x:Name="WindowGrid" Background="#FF101010" MouseDown="Grid_MouseDown">

        <Grid.RowDefinitions>
            <RowDefinition Height="5*">
            </RowDefinition>
            <RowDefinition Height="95*">
            </RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock 
            Text="Slɪ́k"
            Padding="10px"
            TextAlignment="Left"
            VerticalAlignment="Center"
            FontFamily="{StaticResource SpaceMono}"
            FontSize="32">
            <TextBlock.Foreground>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Offset="0" Color="#C7E4FF"/>
                    <GradientStop Offset="1" Color="#83C3FF"/>
                </LinearGradientBrush>
            </TextBlock.Foreground>
        </TextBlock>


        <TextBlock 
            Padding="10"
            Foreground="#616161"
            TextAlignment="Center"
            VerticalAlignment="Center"
            FontFamily="{StaticResource SpaceMono}"
            FontSize="15">
            <Run Text="File Name - " Foreground="Gray"/>
            <Run Text="Project Name" Foreground="White"/>
        </TextBlock>


        <Button x:Name="CloseButton" Click="CloseButton_Click" Margin="0,10,10,10" Style="{StaticResource ControlStyle}">
            <Image Source="{DynamicResource CloseIcon}"/>
        </Button>

        <Button x:Name="MinimizeButton" Margin="1530,0,0,0" Style="{StaticResource ControlStyle}" HorizontalAlignment="Left" Height="20" Click="MinimizeButton_Click">
            <Image Source="{DynamicResource MinimizeIcon}"/>
        </Button>


        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Row="0" Style="{StaticResource PanelStyle}"/>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="9*"/>
                </Grid.RowDefinitions>


                <Grid Grid.Row="1">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>


                    <Border Grid.Row="1" Style="{StaticResource PanelStyle}">
                        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Focusable="False">
                            <TextBox  Style="{StaticResource TextBoxStyle}">
                                Press the + to import from file or clipboard dddddddddddddddddddddddddddddddddddd

                            </TextBox>
                        </ScrollViewer>
                    </Border>
                    
                    <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource PanelStyle}"/>
                    <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource PanelStyle}"/>

                </Grid>
                <Border Grid.Row="0" Style="{StaticResource PanelStyle}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
